spring:
  datasource:
    username: macbookpro
    password: 19801980
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/todo_app

  jpa:
    hibernate:
      ddl-auto: update


  # Message broker

  # malumotni yaxlitliga
  # async
  # masshtablilik (pub/sub)

  # client -> service1 ->  (mb(data))->  service2 (notfy service)

  #